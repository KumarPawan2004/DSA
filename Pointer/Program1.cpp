#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{

    // Initialize a variable and a pointer
    // int a = 10;
    // Declare a pointer variable
    // int *p;
    // Assign the address of a to pointer variable p
    // p = &a;

    // Print the address of a using pointer variable p and & operator
    // cout << "Address of a is " << &a << endl;

    // cout << "Address of a is " << p << endl;

    // printf("Address of a is %d %d\n", p, &a);

    // Print the value of a using pointer variable p and dereference operator
    // cout << "Value of a is " << a << endl;

    // cout << "Value of a is " << *p << endl;

    // printf("Value of a is %d\n", *p);




    
    return 0;
}